:this-action: настройки подключения модуля {wf}
:anchor: wf
:module: модуля {wf}
:link-part: workflow

= Группа настроек "Подключение"

Данный раздел документации {of-mc} описывает группу настроек подключения модуля {wf} к серверу приложений.

include::partial$excerpts.adoc[tags=mc-extension]

Подключение к серверу приложений настраивается в {of-mc} на странице menu:Серверы[Имя-сервера > {wf} > Подключение > Сервер приложений]. Интерфейс страницы настроек модуля {wf} представлен в виде списка. Настройки представлены универсальными компонентами редактирования.

.Группа настроек "Подключение"
image::workflow-connection.png[Группа настроек "Подключение"]

[#appserver]
:parameter: ServiceUrl
Сервер приложений::
Раскрывающийся список, позволяющий выбирать зарегистрированный сервер приложений, к которому подключается сервис Workflow. По умолчанию Сервер приложений в списке не выбран, в качестве заполнителя отображается название настройки: "Сервер приложений"footnote:[
include::partial$excerpts.adoc[tags=analog-param]
].
+
include::partial$excerpts.adoc[tags=select-appserv]
"<<handling,Доля процессов>>" и "<<mail-conn,Почтовое соединение>>".

:proga: {cns}", раздел "{wf}
[#handling]
Доля процессов::
С помощью компонента редактирования можно настроить пропорции распределения бизнес-процессов для каждой БДfootnote:[
include::partial$excerpts.adoc[tags=analog-program]
].
Чтобы список БД обновился, выберите другой "<<appserver,Сервер приложений>>" и примените настройки.
+
Для каждой БД в поле вводится число, определяющее относительную нагрузку на сервис Workflow от `0` до `1000`. Например, при доле `0` сервис Workflow не обрабатывает БП, при долях `3` и `4` нагрузка делится как `43%` на `57%`. Для БД, назначенной по умолчанию, добавляется тэг "БД по умолчанию".

:module: {wf}
:parameter: WorkflowServer
:link-part: workflow
[#mail-conn]
Почтовое соединение::
Блоки с раскрывающимся списком для выбора почтового соединенияfootnote:[
include::partial$excerpts.adoc[tags=analog-param]
] конкретной БД (БД, назначеная по умолчанию, обозначается тэгом "БД по умолчанию"). В блоке настроек отображаются имена почтовых соединений, xref:user:connections-mail-server.adoc[настроенных для Службы {ws}].
+
Если значение в списке не выбрано, используется почтовое соединение по умолчанию для данного экземпляра модуля {wf}. Если в сервисе настроек почтовые соединения недоступны, вместо списка отображается текст: `Нет доступных почтовых соединений`.
+
Чтобы список БД обновился, выберите другой "<<appserver,Сервер приложений>>" и применените настройки.

:parameter: WorkflowServer
[#default-mail]
Почта по умолчанию::
В списке отображаются названия почтовых соединенийfootnote:[
include::partial$excerpts.adoc[tags=analog-param]
] из xref:user:connections-mail-server.adoc[сохранённых в Сервисе настроек]. Выбранное в списке соединение назначается почтовым соединением по умолчанию для данного экземпляра модуля {wf}. Если соединение по умолчанию выбрано, оно будет использоваться. Если в сервисе настроек почтовые соединения недоступны, вместо списка отображается текст: `Нет доступных почтовых соединений`.

:parameter: APIKEY
API-ключ для подключения к серверу::
Используется для аутентификации в API данного сервиса {wf} Сервера приложенийfootnote:[
include::partial$excerpts.adoc[tags=analog-param]
]. Можно оставить значение по умолчанию или изменить, главное условие, чтобы значения в конфигурации модулей {pl} и {wf} были одинаковые. Обязательная настройка.
+
include::partial$excerpts.adoc[tags=use-cryption]

:parameter: SystemUserAccount
Имя системного пользователя::
Имя системной учётной записи {dv}footnote:[
include::partial$excerpts.adoc[tags=analog-param]
]. Обязательная настройка.

:parameter: SystemUserPassword
Пароль системного пользователя::
Парольfootnote:[
include::partial$excerpts.adoc[tags=analog-param]
] системной УЗ для взаимодействия с Сервером приложений.
+
include::partial$excerpts.adoc[tags=use-cryption]

[#cert-stamp]
:module: {wf}
:parameter: DataProtectCertificateThumbprint
:mdl-encr-req: {wf-encr-req}
Штамп сертификата шифрования паролей::
Отпечаток закрытого ключа шифрования SHA1footnote:[
include::partial$excerpts.adoc[tags=analog-param]
]. Указывается без двоеточия, например, `D8602179888DC8402B393F11DCA16A3376DDF879`, см. подробнее раздел "xref:dv6@engineer:ROOT:appendix/account-protection.adoc[]" в руководстве по настройке системы. +
Если шифровать пароль системной учётной записи не планируется, параметр можно не указывать.
+
include::partial$excerpts.adoc[tags=setting-removed]

:parameter: MemorySize
Лимит памяти, Мбайт::
Объем оперативной памяти, выделенной для сервиса Управления процессамиfootnote:[
include::partial$excerpts.adoc[tags=analog-param]
]. Минимальное значение `4096` Мб.

[#isol-mode]
:parameter: IsolationMode
Режим изоляции::
По умолчанию режим активирован. Флаг включает режим, при котором каждому процессу выделяется личная сессия и личный набор шлюзов на время обработки, что исключает влияние задержек и блокировок со стороны других процессов.footnote:[
include::partial$excerpts.adoc[tags=analog-param]
].
+
Когда флаг `*Режим изоляции процесса*` установлен, сессия и набор шлюзов будут находиться в эксклюзивном использовании процесса на время его обработки. Режим необходим для того, чтобы задержки и блокировки, возникающие в сессии/шлюзе из-за использования их неким процессом, по возможности (зависит от реализации шлюза), не влияли на другой процесс.
+
[NOTE]
====
Используемые по умолчанию настройки сервиса Workflow оптимальны для большинства случаев. Менять их рекомендуется только после консультации со службами технической поддержки или консалтинга _{dv}_.
====
